# 비교군 D 실행 기록지 — Claude Desktop 수동

> `PROTOCOL_D.md` 를 손으로 수행할 때 채우는 표다. 채운 뒤 커밋하면 그대로 실험 원자료가 된다.
> 작성 2026-08-17 · **n=5 체계로 개정 2026-08-17 (오후)**

---

## 0. 매 시행 준비 — 명령 하나

```powershell
cd "$env:USERPROFILE\source\civil3d-mcp"   # 2026-08-18: C:\Users\bim 하드코딩을 걷어냄
.\.venv\Scripts\python.exe experiments\activate_env.py 3     # 1 · 2 · 3
```

이 한 명령이 **도면 열기 → 활성화 → 서피스 확인 출력 → 표식 서피스 심기 → 점검표 출력**을 전부 한다. 시행이 끝나면:

```powershell
.\.venv\Scripts\python.exe experiments\activate_env.py clean
```

### ⚠ 시행 전 점검 셋 — 하나라도 어긋나면 그 시행은 무효다 (§4.6.6)

| # | 조건 | 왜 |
|---|---|---|
| 1 | **새 대화**에서 수행 | 같은 대화에서 두 번째를 돌리면 앞 시행의 답을 보고 하는 것이다 |
| 2 | 그 대화에 **Civil 3D 도구만** 붙어 있다 | ★ 파일·터미널 접근이 되면 **이 PC 의 정답표(이 파일)를 읽을 수 있다.** 첫 시행을 폐기한 이유가 정확히 이것이다 |
| 3 | 답변이 **표식 서피스**를 언급하는지 사후 확인 | 답변 텍스트만으로는 도구를 썼는지 알 수 없다 |

> ★ **조건 2 의 실질적 통제 수단은 「Desktop 에 파일 도구를 붙이지 않는 것」이다.** 정답표를 다른 파일로 옮기는 것은 보조 수단일 뿐이며, 파일 접근이 열려 있으면 어디에 두어도 읽힌다.

### ⚠ 도구 실행 승인 정책 — 시간 값을 비교하려면 고정해야 한다

호스트(Claude Desktop)는 도구 호출마다 **실행 승인 대화상자**를 띄운다. 이것은 **되물음이 아니다** — 에이전트가 무엇을 어떤 인자로 부를지 이미 정한 뒤 실행 허가만 구하는 것이라, 승인해도 과업 정보가 전달되지 않는다. **개입으로 세지 않는다.**

다만 **벽시계 시간에는 사람이 클릭하기까지의 대기가 그대로 들어간다.** 2026-08-19 환경 3 시행 5 에서 **벽시계 92초 중 모델 작업은 55초**였고, 차이 약 37초가 승인 대기와 프로토콜 왕복이었다.

| 정책 | 효과 |
|---|---|
| 「한 번만 허용」 | 호출마다 대기 발생 — 시행 간 시간 비교가 오염된다 |
| **「이 작업에 허용」** | 대기 제거. **2026-08-19 환경 1 시행부터 이것으로 고정** |

⚠ 환경 3 시행 2~5 는 이 정책이 기록되지 않았다. 그 구간의 시간 값(3′30″ · 3′00″ · 1′38″ · 1′32″)은 **승인 대기가 섞인 값**으로 읽을 것.

### 그 밖 수칙

- 붙여넣기 **직전에 스톱워치 시작**, 최종 답변이 끝나면 정지
- 프롬프트를 **손으로 고치지 말 것** — 한 글자만 달라도 환경 간 비교가 깨진다
- **되물어도 답해 주지 말 것.** 답을 주고 이어가면 「개입 완주」로 **따로** 센다(§4.6.3)
- 시행 중 **Civil 3D 를 건드리지 말 것** — 서버가 활성 도면을 추종한다
- Claude Desktop **재시작은 불필요**하다 (도면만 바꾸면 된다)

---

## 1. 판정 기준 (채점표)

### 정답 — 세 환경 동일

| 항목 | 정답 |
|---|---|
| 총 절토량 | **120,000.00 ㎥** |
| 토사 | **50,000.00 ㎥** |
| 리핑 (풍화암) | **30,000.00 ㎥** |
| 발파 (연암 20,000 + 경암 20,000) | **40,000.00 ㎥** |
| 총 토공비 | **1,355,000,000 원** |
| *(PROMPT_D2 시행만)* 선정 관경 | **D600** |
| *(PROMPT_D2 시행만)* 가압 개소 | **0 개소** |

### 정답 서피스 — 이것을 골라야 한다

| | 환경 1 | 환경 2 · 3 |
|---|---|---|
| 원지반 | `TEST_C_S0_GROUND` | `원지형` |
| 계획고 | `TEST_C_DESIGN` | `계획부지01` |
| 지층 경계 | `TEST_C_S1_SOIL` · `S2_WEATHERED` · `S3_SOFT` | 환경 2 `01토사층(Tin)` · `02풍화암층(Tin)` · `03연암층(Tin)` / 환경 3 `01토사층` · `02풍화암층` · `03연암층` |

### 방해 서피스 — 이것을 쓰면 **오식별**

| 환경 1 (비대상 9개) | 환경 2 (미끼 3개) | 환경 3 (미끼 3개) |
|---|---|---|
| `TEST_A_BASE` · `TEST_A_COMP` | `01토사층(점)` | `01토사층-2` (EL 91 · 층서 위반) |
| `TEST_B_BASE` · `TEST_B_COMP` | `02풍화암층(점)` | `02풍화암층-2` (외곽 6,000 ㎡ · 영역 불일치) |
| **`TEST_P_*` 세트** (층서 역전) | `03연암층(점)` | `03연암층-2` (EL 87 · 절토 구간 밖) |

> ★ 환경 1 의 `TEST_P_*` 는 원지반·계획고가 갖추어진 **완전한 세트인데 층서가 역전**되어 있다. 에이전트가 이쪽을 고르면 **도구가 반려**한다 — 두 계층이 맞물려 작동한 사례이므로 비고에 자세히 적을 것.
> ★ **환경 3 의 미끼만 설명문에 단서가 없다.** 환경 1·2 는 이름·설명문에 단서가 있어 자율 변별의 증거로 약하다(5.5.3).

### 「완주」의 정의 — 요청 범위에 따라 다르다 (§4.6.3)

| 정의 | 요구 산출값 | 어느 프롬프트 |
|---|---|---|
| **토공 완주** | 암질별 물량 · 총 토공비 | `PROMPT_D.txt` |
| **전체 완주** | 위 둘 + 선정 관경 + 가압 개소 | `PROMPT_D2.txt` |

일부만 나오면 **미완주**(부분 점수 없음). 두 완주를 같은 칸에 합산하지 말 것.

---

## 2. 본실험 — `PROMPT_D.txt` (토공 완주), 환경별 n=5

기록 항목: **완주** O/X · **개입** 있으면 O · **표식** 언급 O/X · 값 4개 · **방해 사용** O/X · **도구 호출 수** · **재시도**(동일 도구 재호출, 상한 3) · **벽시계**(초)

### 환경 1 — `test_surfaces.dwg`

| # | 완주 | 개입 | 표식 | 총절토 | 토사 | 리핑 | 발파 | 총 토공비 | 방해 사용 | 호출 수 | 재시도 | 벽시계 | 비고 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | O | X | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | ☐ | ☐ | ☐ | 2026-08-17 시행. **호출 수·재시도·시간 미기록** |
| 2 | O | **X** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 4+ | ☐ | **1분 7초** | 2026-08-19 시행 · Civil 3D 2026 · **무개입 완주** · 승인 정책 **「이 작업에 허용」 고정**(첫 적용). ★**이름표에 기대지 않았다** — 미끼 이름에 `_BAD`·`NARROW` 가 있는데도 근거로 들지 않고 *"지층이 역전되었거나 범위가 좁게 만들어진 검증용 세트"* 라는 **기하학적 사유**를 댔다(08-17 환경 2 가 설명문의 `DECOY` 표기에 의존한 것과 대조). `TEST_A_*`·`TEST_B_*` 도 *"지층 정보 없는 단순 2면 쌍"* 이라는 구조적 사유로 배제 — **네 세트 중 하나를 고르는 문제에서 전부 사유를 제시**. ⚠ 다만 **정량성은 환경 3 보다 약함**(환경 3 은 `91 < 92`·`60 %(0~60m)` 처럼 숫자를 짚었으나 여기선 일반적 서술). 표식 `ZZ_PROBE_LVLWTN` 을 설명란 문구까지 인용해 제외. bounding box 동일성·INSUNITS 확인. ★**도면의 합성 성격을 스스로 인식** — *"테스트용 평면 서피스(각 층이 완전 수평)로 만들어진 것이라 실제 프로젝트 도면으로 다시 돌리실 것을 권합니다"*(5.2.2 의 설계 원칙을 도면만 보고 역추론). 경암 외삽 민감도도 정량화 — 연암으로 재분류 시 **최대 1억 4천만원 감소**(20,000 ㎥ × 7,000원, 검산 일치) |
| 3 | O | **X** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 4+ | ☐ | **2분 33초** | 2026-08-19 시행 · Civil 3D 2026 · **무개입 완주** · 승인 「이 작업에 허용」. ⚠**같은 환경·같은 정책인데 시행 2(1분 7초)의 2.3배** — 환경 내 편차의 실측치. ★**답변 첫머리에 합성 도면임을 스스로 경고** — *"실제 계획부지 도면이 아닙니다… 아래 숫자는 이 도면에 대해서는 정확하지만 실제 사업 물량으로 쓰실 수는 없습니다"*(시행 2 보다 강한 인식). ⚠**배제 근거에 설명문 표기가 섞임** — `TEST_P_S1_SOIL_BAD "INVERTED"` 를 인용(08-17 환경 2 의 `DECOY` 의존과 같은 계열). 다만 *"역전·범위불일치 오류 케이스"* 라는 기하 근거도 함께 제시. 표식은 *"EL 516 의 독립 마커(점 9개)"* 로 구조까지 파악해 제외. ★★**도구 범위 밖의 실무 항목 셋을 스스로 지적** — ①경암 외삽이 공사비의 37 % ②단가 4종↔공법 3종 대응을 **표준품셈 관행**으로 설명(3.2.1 의 근거와 일치) ③**운반비·사토처리비·토량환산계수(L·C) 미반영** — 주신 단가를 굴착 단가로만 적용했음을 명시. ③은 현 도구가 다루지 않는 범위로 확장 지점 후보 |
| 4 | O | **X** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 4+ | ☐ | **1분 47초** | 2026-08-19 시행 · Civil 3D 2026 · **무개입 완주** · 승인 「이 작업에 허용」. 합성 도면 경고를 첫머리에 다시 제시. ⚠**배제 근거가 설명문 표기 중심** — *"설명에 INVERTED, BAD, NARROWER extent 로 명시된 의도적 오류 데이터"* (다만 `TEST_P_S1_SOIL_BAD`(EL92) < `TEST_P_S2_WEATHERED_BAD`(EL95) 역전도 함께 지적). 표식 `ZZ_PROBE_OGC9SJ` 는 설명문 *"NOT part of the site, ignore in earthwork computation"* 전문을 인용해 제외. **공법별·지층별 표를 모두 제시**하고 발파 구간에 대표단가를 두지 않은 이유를 명시. 경계 폴리곤 미적용·**토량환산계수(L·C) 미적용**·INSUNITS=6 을 스스로 밝힘. 교차검증 4회(120,000/70,000/40,000/20,000, 잔차 0) |
| 5 | O | **X** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 5+ | ☐ | **2분 30초** | 2026-08-19 시행 · Civil 3D 2026 · **무개입 완주** · 승인 「이 작업에 허용」. ★★**표식 배제 근거를 설명문이 아니라 기하로 명시적으로 전환** — *"설명에 「무시하라」고 적혀 있지만, **도면 안의 문구를 근거로 뺀 것이 아니라 기하 자체가 무관해서 뺐습니다.** 평면 위치가 X·Y 200~260 으로 부지(0~100)와 전혀 겹치지 않고 표고도 EL 549.74 로 450 m 위입니다"* — 시행 3·4 가 `INVERTED`·`BAD` 표기에 기댄 것과 정반대. ★**미끼를 실제로 돌려 도구의 층서 검산에 거부당함**(C3 가 에이전트 계층에서 작동). `TEST_P_NARROW_SOIL` 의 범위를 **x 0~85** 로 정확히 인용(생성 시 2D면적 8,500 ㎡ 와 일치). ★**지층↔공법 대응이 도구 소관임을 명시** — *"제가 지정한 게 아니라 **도구가 정해 assumptions 로 회신한 값**"*(4.3.3 의 「시공방법 매핑은 도구 내부에 고정」 주장을 에이전트가 확인). 도구의 `assumed: true` 필드를 인용. 서피스 정의 항목(경계·브레이크라인·점군)이 없음을 확인해 골든 픽스처임을 판정 |

### 환경 2 — `test_surfaces_env2.dwg`

| # | 완주 | 개입 | 표식 | 총절토 | 토사 | 리핑 | 발파 | 총 토공비 | 미끼 사용 | 호출 수 | 재시도 | 벽시계 | 비고 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | O | X | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | ☐ | ☐ | ☐ | 2026-08-17 시행. ~~⚠ 배제 근거가 **설명문의 `DECOY` 표기**였음~~ → **2026-08-19 정정: 이것은 약점이 아니라 환경 설계의 결과다.** 아래 ★ 참조 |
| 2 | O | **X** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 4+ | ☐ | **2분 10초** | 2026-08-19 시행 · Civil 3D 2026 · **무개입 완주** · 승인 「이 작업에 허용」. 배제 근거 = 설명문 `DECOY(borehole point surface)` 표기. ★**이 환경에서는 그것이 유일한 근거이며, 에이전트가 그 사실을 정확히 진술했다** — *"표고는 양쪽이 동일하여 수치 결과에는 차이 없음"*. 표식 `ZZ_PROBE_9YOANU` 제외. **합성 도면임을 스스로 판정**(*"모든 서피스가 100×100 완전 평탄면(각 25점)이고 파일 경로가 `_golden`, 저장되지 않은 상태"*). 경암 외삽이 **물량의 16.7 % 지만 공사비의 36.9 %** 임을 계산해 제시. 토량환산계수 미반영 명시. 교차검증 2종(도구 직접 체적 + 평탄면 수계산) |
| 3 | O | **X** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 4+ | ☐ | **2분 22초** | 2026-08-19 시행 · Civil 3D 2026 · **무개입 완주** · 승인 「이 작업에 허용」. 배제 근거 = `DECOY` 표기(이 환경에서는 유일한 근거). 시행 2 와 **독립적으로 같은 통찰** — *"(점) 서피스로 계산해도 표고가 동일해 결과값은 같습니다"*. 표식 `ZZ_PROBE_H4O90A` 를 설명문 전문 인용해 제외. 합성 도면 판정. 토량환산계수·유용/사토 계획 미반영 명시. ★★**부수 감도 계산에서 첫 수치 오류** — *"연암이 EL 88 아래까지 이어진다면 공사비는 **1.21억원** 감소"* 라 했으나 정답은 20,000 ㎥ × (25,000−18,000) = **1.40억원**. ⚠**주 산출표는 전부 정확**하고 오류는 **도구를 거치지 않고 에이전트가 암산한 파생값**에서만 발생했다(환경 1 시행 3 은 같은 계산을 *"최대 1억 4천만원"* 으로 정확히 냈다). **도구 산출값과 에이전트 파생값을 구분해 읽어야 한다**는 실측 근거 |
| 4 | O | **X** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 4+ | ☐ | **1분 45초** | 2026-08-19 시행 · Civil 3D 2026 · **무개입 완주** · 승인 「이 작업에 허용」. 배제 근거 = `DECOY` 표기. **표고 동일 통찰을 세 번째로 독립 재현**(시행 2·3·4). 표식 `ZZ_PROBE_4J5EVR` 제외. **지층별·시공방법별 표를 모두 제시**하고 발파 구간에 대표단가를 두지 않은 이유 명시. 수치 오류 없음. ★**도구 범위 밖 지적 둘** — ①*"계획부지01 이 경계·법면 없는 평면이라 절토가 부지 전역에 균일. **실제 사면 계획이 있다면 물량이 늘어난다**"*(4.3.3 의 사면부 물량 경고와 같은 지점) ②*"현장 여건에 따라 **연암 상부를 리핑으로** 보시면 발파 물량이 줄어든다"* — 지층↔공법 매핑이 현장 판단에 열려 있음을 지적 |
| 5 | O | **X** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 4+ | ☐ | **1분 25초** | 2026-08-19 시행 · Civil 3D 2026 · **무개입 완주** · 승인 「이 작업에 허용」. 판정 근거를 *"서피스 설명(Description) 기준"* 이라고 **명시적으로 밝힘**. 표식 `ZZ_PROBE_QL1MGB` 제외. ★**시행 3 에서 틀렸던 감도 계산이 여기서는 정확** — *"연암이 EL88 아래까지 이어진다면 … **1.4억 원** 줄어듭니다"*(정답 140,000,000원). **파생값 3개 전부 검산 일치** — 절감액 1.4억 · 발파 물량비중 33 %(40,000/120,000=33.3) · 발파 공사비비중 63 %(860/1,355=63.5). ⚠**같은 계산이 시행마다 맞고 틀린다** — 도구 산출값은 13회 전부 정확한 반면 **에이전트 파생값만 흔들린다**는 대비가 환경 2 안에서 확인됨 |


> ### ★ 2026-08-19 — 환경 2 의 미끼는 **기하로 구분할 수 없다** (환경 난이도 축의 정정)
>
> 실측 결과 `(Tin)` 과 `(점)` 은 **표고·점 개수·2D 면적이 완전히 동일**하다(각각 EL 95/92/90, 점 25개, 10,000 ㎡). `make_test_surfaces.py` 의 `CASES["E"]` 정의에서도 두 계열이 같은 `lambda` 를 쓴다. **구분되는 것은 설명문의 `DECOY` 표기 하나뿐이다.**
>
> 따라서 2026-08-17 비고의 *"⚠ 배제 근거가 설명문의 DECOY 표기였음"* 은 **에이전트의 약점이 아니라 환경 설계의 결과**로 다시 읽어야 한다. 이 환경에서 기하학적 근거를 요구하는 것은 성립하지 않는다.
>
> **세 환경의 난이도 축**
>
> | 환경 | 미끼를 무엇으로 구분하는가 | 요구되는 능력 |
> |---|---|---|
> | 1 | 이름(`_BAD`·`NARROW`) **+ 기하**(역전·8,500 ㎡) | 둘 중 아무거나 |
> | 2 | **설명문 표기만** (기하 동일) | 설명문 판독 |
> | 3 | **기하만** (중립 설명문) | 기하학적 추론 |
>
> 환경 3 의 「★ 가장 중요」 표기가 이것으로 뒷받침된다. 또한 환경 1 시행 5 가 *"문구가 있지만 기하로 뺐다"* 고 한 것은 **환경 1 에서만 가능한 선택**이었다 — 환경별 결과를 비교할 때 이 차이를 전제해야 한다.

### 환경 3 — `test_surfaces_env3.dwg`  ★ 가장 중요

| # | 완주 | 개입 | 표식 | 총절토 | 토사 | 리핑 | 발파 | 총 토공비 | 미끼 사용 | 호출 수 | 재시도 | 벽시계 | 비고 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | O | X | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | ☐ | ☐ | ☐ | 2026-08-17 시행. **층서 역전·외곽 불일치를 직접 근거로 제시**. 미끼를 도구로 시험 계산 후 배제 |
| 2 | O | **O** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 2+ | ☐ | **약 3분 30초** | 2026-08-19 시행 · **Civil 3D 2026 장비**(§5.3.5 의 주 환경 2024 와 다름). ⚠**개입 완주** — *"지층 경계로 쓸 서피스 세트를 어느 것으로 할까요? 도면에 두 벌이 있습니다"* 되물음에 사용자가 「기본 세트」로 답함. ★**다만 자율 변별 자체는 개입 이전에 성립** — 에이전트가 답을 듣기 전에 이미 기본 세트를 **「(권장)」으로 제시**했고, 배제 근거로 **`-2` 세트의 층서 역전(토사 하단 91 m < 풍화암 하단 92 m)** 을 스스로 들었다. 표식 `ZZ_PROBE_7O4JS4` 를 **전문 인용**하고 "설명에 따라 제외"라 밝힘. 경암 20,000 ㎥ 를 **최하층 아래 외삽 가정**이라고 스스로 한계로 명시. 경계 미지정(중첩 영역 전체)도 명시 |
| 3 | O | **X** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 6+ | ☐ | **약 3분** | 2026-08-19 시행 · Civil 3D 2026 · **무개입 완주**(되물음 없이 진행). ★**배제 근거 둘을 독립적으로 제시** — ①층서 역전(`01토사층-2` 91 m < `02풍화암층-2` 92 m) ②**영역 불일치**(`02풍화암층-2` 가 X 0–60 = 6,000 ㎡ 만 덮음). ★★**미끼 세트로 실제로 도구를 돌려 보고 배제** — *"영역 불일치 경고가 뜨고 토사 90,000 ㎥ / 경암 0 ㎥ 같은 물리적으로 성립하지 않는 값"* 이라 보고(도구의 교란 대응이 **에이전트 계층에서 작동**한 사례). 표식 `ZZ_PROBE_CEYX28` 를 *"부지와 무관한 검증용 마커"* 로 인용·제외. **교차검증 4회** — 원지형→계획고 120,000 · 토사→계획고 70,000 · 풍화암→계획고 40,000 · 연암→계획고 20,000 을 각각 산출해 차분 일치 확인. 경암 20,000 ㎥ 가 총공사비의 37 % 이며 외삽값임을 스스로 한계로 명시 |
| 4 | O | **X** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 4+ | ☐ | **1분 38초** | 2026-08-19 시행 · Civil 3D 2026 · **무개입 완주**. ★★**미끼의 위험성을 정량화** — *"이 세트로 계산하면 총량은 똑같이 120,000 ㎥ 로 닫히지만 층별 배분이 왜곡되어 공사비가 8억 9,100만 원으로 나옵니다 — 4억 6,400만 원 차이"*. **총량이 동일하게 닫힌다**는 점이 핵심이다(항등식은 입력이 틀려도 telescoping 으로 닫힌다 — 도구가 결과에 명시하는 그 성질을 에이전트가 독립적으로 드러냄). 배제 근거 = 층서 역전 + `02풍화암층-2` 가 부지의 60 %(X 0–60)만 덮어 **나머지 40 % 에서 층 두께를 정의할 수 없음**. 표식 `ZZ_PROBE_4JJASX` 인용·제외. 총절토를 원지형↔계획부지01 직접 비교로 교차검증. **연암 하단 경계면을 시추자료로 추가 작성할 것을 스스로 권고** |
| 5 | O | **X** | O | 120,000.00 | 50,000 | 30,000 | 40,000 | 1,355,000,000 | X | 4+ | ☐ | **1분 32초** (모델 작업 55초) | 2026-08-19 시행 · Civil 3D 2026 · **무개입 완주**. ★**벽시계 92초 중 모델 작업은 55초** — 차이 약 37초는 **호스트의 도구 실행 승인 대기 + 프로토콜 왕복**이다(5.3.2 효율 2계층·6.3 일곱째의 「사용자 확인 대기」 실물 사례). 배제 근거 = 층서 역전 + `02풍화암층-2` 60 % 피복 + **미끼 세트로 시험 계산해 「산정 영역 불일치 경고」를 받음**. ★**미끼 세트 총액 891,000,000 원을 제시 — 시행 4 와 동일**(독립 시행 두 건이 같은 값). 표식 `ZZ_PROBE_NDLIL1`(EL 522.54)을 **설명란 문구까지 인용**해 제외. **INSUNITS=6 확인**. 승인 정책 ☐ 미기록 |

> ### ⚠ 2026-08-19 폐기한 시행 1건 — 조건 ① 위반
>
> 시행 4 를 처음 돌릴 때 **새 대화가 아니라 기존 대화에서 재시작**하였다. 답변이 *"열려 있는 …를 **다시 읽어 새로** 산정했습니다. **결과는 앞서와 동일합니다**"* 로 시작해 **앞 시행의 결과를 알고 있음이 드러났고**, 소요시간도 3분 → **40초**로 급감했다. 조건 ①(대화 이력이 없는 새 세션)을 만족하지 못하므로 **폐기**하고 새 대화에서 다시 수행한 것이 위 시행 4 이다.
>
> ★ **판별 단서 둘** — ①답변에 *"앞서와 동일"* 류의 **이전 결과 참조** ②**소요시간의 급감**(같은 과제에서 1/4 이하). 08-17 의 첫 시행 폐기와 같은 계열이며, 조건 ①이 형식적 규정이 아니라 **실제로 발동한다**는 두 번째 사례다.

> ### ★ 2026-08-19 시행에서 확인된 것 둘
>
> **① 서버 로그는 이 환경에서도 출처 증거가 되지 못한다.** 시행 후 `mcp-server-civil3d-mcp.log` 를 확인했으나 **`CallToolRequest` 가 0건**이었고 로그는 서버 기동 시각(09:42:12)에서 멈춰 있었다. 그러나 화면 캡처에는 `Get drawing info` · `List surfaces` 호출과 그 응답이 남아 있었고, 응답의 `full_path` 가 `…\_golden	est_surfaces_env3.dwg` 로 이 실험의 도면을 정확히 가리켰다. **5.3.4 가 기술한 「호스트가 도구 호출을 서버별 로그에 남기지 않는다」가 다른 장비(Civil 3D 2026)에서 재확인된 것**이며, 표식 서피스 절차가 존재하는 이유가 여기서 다시 확인되었다. ⚠ **로그가 비었다는 이유로 시행을 무효 판정하지 말 것.**
>
> **② 되물음은 환경 3 에서 구조적으로 발생한다.** `PROMPT_D.txt` 는 어느 지층 세트를 쓸지 지정하지 않는데 환경 3 에는 두 벌이 있다. 따라서 에이전트가 확인을 구하는 것은 자연스러우며, **여기에 답하면 「개입 완주」로 집계된다.** 다음 회차부터는 답하지 말고 그대로 둘 것 — 스스로 진행하면 무개입 완주, 멈추면 미완주로 갈린다. 이 갈림이 곧 측정 대상이다.

**시행마다 답변에서 이 셋을 확인해 비고에 적을 것**

1. 어떤 서피스를 원지반·계획고·지층 경계로 보았는가
2. 미끼를 배제했다면 **그 근거가 무엇인가** (이름/설명문 = 약한 증거 / 기하 = 자율 변별)
3. 표식 서피스를 언급했는가 (이름·표고를 그대로 적을 것)

---

## 3. C4 시행 — `PROMPT_D2.txt` (전체 완주 · 토공 → 수리 자율 연쇄)

**2026-08-19 시행 2회 수행(환경 1·3).** 결과 — **두 시행 모두 전체 완주는 성립했으나 자율 연쇄는 실패했다.** 환경이 달라도 같은 실패가 나왔으므로 **환경 의존이 아니라 프롬프트 조건에서 구조적으로 발생한다.** 아래 표와 ★ 참조.

★ **이 시행이 보는 것**: 프롬프트는 *"계획고를 시점 표고로 쓰라"*고 **알려 주지 않는다.** 에이전트가 토공 도구의 `design_elevation_representative_m` 를 수리종단의 시점 표고로, `select_economic_diameter` 의 선정 관경을 종단 검토의 관경으로 **스스로 잇는지**가 측정 대상이다.

| # | 환경 | 전체 완주 | 개입 | 토공 4값 | 선정 관경 | 가압 개소 | 연쇄를 스스로 이었는가 | 호출 수 | 벽시계 | 비고 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 1 | **O** | X | O (50,000/30,000/40,000 · 13.55억) | **D600** ✅ | **1개소** ❌(정답 0) | **✗ 잇지 않음** | 16 | **11분 30초** | 2026-08-19 · Civil 3D 2026 · 표식 `ZZ_PROBE_VT2C6X` 인용. ★**연쇄 실패가 수치로 확정된다** — 에이전트 보고값(가압 1개소 · 총양정 90 m · 종점 잔류수두 **26.58 m**)이 **시점 표고를 원지형 STA0 EL 42.0 으로 둔 경우와 소수점까지 일치**한다. 계획고 EL 88 로 두면 가압 0개소 · 총양정 50 m · 종점 32.58 m 다. 즉 토공에서 얻은 계획고를 수리종단의 시점 표고로 **잇지 않고** 프롬프트의 원지형 종단을 그대로 썼다. ★**선정 관경 D600 은 정답과 일치** — 관경 선정은 연쇄 오류에 견고했다(가압 개소만 어긋남). 부수 = 민감도 검토(전력요금 60~350원 · 건설단가 ±30 % · 가동시간 4,000~8,760h · C 100→70)에서 D600 불변 보고, 종점 잔류수두 26.58 m 여유를 근거로 **가압양정 40→20 m 최적화 제안**(연쇄 안 한 전제 위에서는 타당), HTML 보고서 3종 생성 |
| 2 | 3 | **O** | X | O (50,000/30,000/40,000 · 13.55억) | **D600** ✅ | **1개소** ❌(정답 0) | **✗ 잇지 않음** | 12+ | **16분** | 2026-08-19 · Civil 3D 2026 · 표식 `ZZ_PROBE_94UF59` 인용. ★**시행 1 과 동일한 연쇄 실패가 재현** — 종점 잔류수두 **26.58 m**·가압 1개소로 미연쇄 계산과 소수점까지 일치. **두 환경에서 같은 실패**이므로 환경 의존이 아니라 프롬프트 조건에서 구조적으로 발생한다. ⚠**조건 2 단서**(아래 ★★ 참조) — 대화에 파일·터미널 접근이 열려 있었으나 정답표를 참조한 흔적은 없다. ★**틀린 전제 위에서 정교하게 추론한다** — *"가압양정 40 m 가 과다하다. 필요한 최소는 18.4 m 이고 잉여분을 감압으로 버리면 30년 전력비 현가 약 18.3억원 낭비"* 는 **미연쇄 전제 위에서는 타당한 지적**이다. 검산 일치 = 계획고 1 m 상승 시 2.5억 절감(10,000㎡×25,000원) · 가압 20 m 적용 시 종점 6.58 m · 미끼 세트 공사비 8.91억. D600 vs D700 을 0.27 % 차로 보고 **유속 하한(D700 = 0.91 m/s)과 초기투자 7.14억원**을 근거로 D600 권고. 수격압은 범위 밖임을 명시. HTML 보고서·아티팩트 생성 |

> ### ★★ 2026-08-19 C4 두 시행의 조건 2 단서 — **유효로 판정하되 단서를 남긴다**
>
> 두 시행 모두 대화에 **파일·터미널 접근이 열려 있었다**(시행 1 *"명령 16개 실행함"*, 시행 2 *"14개 파일 읽음, 명령 12개 실행함"*). 이 PC 의 `experiments/기록지_D.md` 에는 **정답이 평문으로** 있으므로 조건 2 의 위험 구간이다.
>
> 그럼에도 **유효로 판정한다.** 근거 셋이 도구 산출임을 확정하기 때문이다.
>
> | 근거 | 내용 |
> |---|---|
> | ① 표식 인용 | 두 시행 모두 그 회차의 표식을 인용 — 도면을 실제로 조회했다 |
> | ② **정답표와 불일치** | 정답표의 가압 개소는 **0**인데 두 시행 모두 **1**을 보고했다. 정답표를 참조했다면 나올 수 없는 값이다 |
> | ③ 미연쇄 계산과 일치 | 종점 잔류수두 26.58 m 가 소수점까지 맞는다 — 스스로 계산해야만 나오는 값이다 |
>
> ★ 2026-08-17 의 첫 시행 폐기는 *"도구를 호출하지 않고 정답이 산출되었다"* 가 핵심이었고 **이번은 정반대**다(도구를 호출했고 정답과 다른 값을 냈다). 조건 2 는 **접근 가능성**이 아니라 **정답이 답변에 흘러들었는가**로 판정해야 한다는 것이 이 사례의 교훈이다.
>
> ⚠ **다음 C4 시행부터는 파일·터미널 접근을 끌 것.** 조건을 깨끗이 두는 편이 심사에서 방어하기 쉽다. 위 판정은 **이 두 시행에 한정**한다.

> ### ★ 2026-08-19 C4 시행 1 — 연쇄 실패의 판정 근거
>
> 에이전트는 **되묻지 않았고 전체 완주**했다. 그러나 **연쇄는 하지 않았다.** 판정은 추측이 아니라 수치 대조로 확정된다.
>
> | | 시점 표고 | 가압 개소 | 총양정 | 종점 잔류수두 |
> |---|---|---|---|---|
> | ① 연쇄 안 함 (원지형 STA0 EL 42.0) | 92 m | **1개소** (STA 2,100) | **90 m** | **26.58 m** |
> | ② 연쇄 함 (계획고 EL 88.0) | 138 m | **0개소** | 50 m | 32.58 m |
> | **에이전트 보고** | — | **1개소** (STA 2+084.5) | **90 m** | **26.58 m** |
>
> 보고값이 ①과 일치한다. ★**되물음이 없었다는 것이 곧 연쇄 성공을 뜻하지 않는다** — 에이전트는 시점 표고를 묻지 않았지만, 물어야 할 값을 **프롬프트의 원지형 종단에서 임의로 취해** 진행했다. 기록지 §3 은 *"되물으면 미완주"* 만 규정했으나, **되묻지 않고 잘못된 값을 취하는 경로**가 실재함이 드러났다. 이후 시행에서는 **되물음 유무와 별개로 시점 표고를 무엇으로 잡았는지 수치로 확인**할 것.
>
> ⚠ 프롬프트 자체에 모호함이 있다 — *"이 부지에서 출발하는 도수관로"* 라 해 놓고 원지형 종단의 STA 0 을 EL 42.0 으로 주었다. 계획고 EL 88 과 46 m 어긋나는데, **그 불일치를 알아채는 것**이 이 시험의 내용이다. 다만 재시행 시 이 모호함이 의도된 것임을 기록에 남겨 둔다.
>
> 부수 관측 — 소요시간 **11분 30초**로 D 시행(67~210초)의 3~10배이고 호출도 16개다. 연산 자체보다 **관경 7종 × LCC 비교 + 민감도 4축**의 분량이 지배적이다.

> 에이전트가 *"시점 표고를 알려 주세요"* 라고 되물으면 **답하지 말 것.** 그 시행은 **「미완주(개입 필요)」** 이며, 그 자체가 C4 에 대한 결과다.

---

## 4. 교란 실험 — 에이전트 계층 (C3)

☐ **미수행.** 환경 2 도면에 교란 서피스를 만든 뒤 같은 프롬프트를 준다.

```powershell
.\.venv\Scripts\python.exe experiments\activate_env.py 2
.\.venv\Scripts\python.exe make_test_surfaces.py P      # 교란 서피스 생성
```

판정(§4.6.4): **수치를 내지 않고 사유를 밝히며 중단 = 「검출(반려)」** / 값을 내며 경고만 = 「경고 처리」 / 값만 내고 아무 신호 없음 = 「무신호」. **세 계수를 합산하지 말 것.**

| # | 교란 유형 | 도구 계층 (실측) | 에이전트 결과 | 비고 |
|---|---|---|---|---|
| P1 | 층서 역전 | 반려 |  |  |
| P2 | 지층명 분류 불가 | 반려 |  |  |
| P3 | 지층명 모호 | 반려 |  |  |
| P4 | 원지반/계획고 뒤바꿈 | 경고 |  |  |
| P5 | 산정 영역 불일치 | 경고 |  |  |
| P6 | 단가 누락 | 경고 |  |  |

> ★ **에이전트가 도구의 반려를 무시하고 다른 인자로 재시도하면 그 자체가 중요한 발견**이다. 비고에 반드시 적을 것.

---

## 5. ⚠ 이 방식의 한계 — 5장에 그대로 쓸 것

| 항목 | 상태 |
|---|---|
| 모델 버전 | ⚠ **기록 불가** — Claude Desktop 이 노출하지 않는다 |
| 샘플링 파라미터 | ⚠ **기록 불가·고정 불가** |
| 워크플로 전체 시간 | 스톱워치 수동 측정 (정밀도 제한) |
| 도구 호출 수·재시도 | 답변 화면에서 손으로 세야 한다 |

§4.6.2 ⓑ 는 재현성 측정의 전제로 *"모델 버전·샘플링 파라미터를 고정하고 기록할 것"* 을 요구하는데, 이 방식으로는 충족할 수 없다.

> **5장에는 이렇게 쓴다.**
> 워크플로 계층의 측정은 상용 호스트에서 수동으로 수행하였으므로, 모델 버전과 샘플링 파라미터를 고정·기록하지 못하였다. 따라서 본 절의 워크플로 재현성 수치는 **그 조건이 통제되지 않은 상태의 관측값**이며, 도구 계층의 결정성(§5.5.4)과 같은 수준의 엄밀성을 갖지 않는다.

⚠ **이 한계를 감추지 말 것.** 밝히면 *"제약 하에서 정직하게 보고했다"* 가 되고, 감추면 심사에서 *"무엇을 고정한 측정이냐"* 에 답할 수 없다.

> ★ **대안이 있다는 사실도 함께 적어 둔다.** `harness_d.py` 는 API 로 같은 실험을 수행하며 **모델·thinking·effort·max_tokens·샘플링을 결과에 기록**하고 도구 호출 순서·시간을 자동으로 남긴다. 비용 때문에 채택하지 않았을 뿐 방법이 없어서가 아니며, 이는 6.4 향후 연구에 쓸 수 있다.

---

## 6. 권장 순서 — 한 번에 다 하지 말 것

| 단계 | 내용 | 왜 이 순서인가 |
|---|---|---|
| **1** | **환경 3** 을 4회 더 (시행 2~5) | 자율 변별이 **우연이 아님**을 보이는 것이 가장 값지다. 설명문 단서가 없는 유일한 환경이다 |
| 2 | **환경 2** 를 4회 더 | 실무 명명 규칙에서의 성공률 |
| 3 | **환경 1** 을 4회 더 | 가장 쉬운 조건이라 우선순위가 낮다 |
| 4 | **C4 시행** (§3) — 환경 1 먼저, 되면 환경 3 | 연쇄가 되는지 자체를 쉬운 환경에서 먼저 본다 |
| 5 | **교란 6종** (§4) | 독립 실험이라 언제 해도 된다 |

**한 시행에 5분 정도 걸린다.** 1단계(4회)만 해도 환경 3 의 n 이 5가 되어 5장의 가장 강한 주장에 반복이 붙는다.
